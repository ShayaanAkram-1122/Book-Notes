<%- include('partials/header.ejs') %>

<style>
/* Home Page Styles */
.home-container {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #2D2D2D;
  overflow-x: hidden;
}

.dark-theme .home-container {
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
  color: #ffffff;
}

/* Hero Section */
.hero-section {
  padding: 80px 20px;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="books" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23books)"/></svg>');
  opacity: 0.3;
  animation: float 20s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
}

.hero-content {
  position: relative;
  z-index: 2;
  max-width: 800px;
  margin: 0 auto;
}

.hero-title {
  font-size: 3.5rem;
  font-weight: 800;
  margin-bottom: 20px;
  background: linear-gradient(45deg, #f39c12, #e74c3c, #9b59b6, #3498db);
  background-size: 300% 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: gradientShift 4s ease-in-out infinite;
}

@keyframes gradientShift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

.hero-subtitle {
  font-size: 1.3rem;
  margin-bottom: 30px;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.6;
}

.dark-theme .hero-subtitle {
  color: rgba(255, 255, 255, 0.8);
}

.hero-buttons {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 40px;
}

.hero-btn {
  padding: 15px 30px;
  border: none;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.hero-btn-primary {
  background: linear-gradient(135deg, #f39c12, #e67e22);
  color: white;
  box-shadow: 0 8px 25px rgba(243, 156, 18, 0.3);
}

.hero-btn-secondary {
  background: rgba(255, 255, 255, 0.2);
  color: white;
  border: 2px solid rgba(255, 255, 255, 0.3);
  backdrop-filter: blur(10px);
}

.hero-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
}

.hero-btn-primary:hover {
  background: linear-gradient(135deg, #e67e22, #d35400);
}

.hero-btn-secondary:hover {
  background: rgba(255, 255, 255, 0.3);
  border-color: rgba(255, 255, 255, 0.5);
}

/* Features Section */
.features-section {
  padding: 80px 20px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
}

.dark-theme .features-section {
  background: rgba(0, 0, 0, 0.2);
}

.features-container {
  max-width: 1200px;
  margin: 0 auto;
}

.section-title {
  text-align: center;
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 60px;
  color: white;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 30px;
  margin-top: 50px;
}

.feature-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  padding: 40px 30px;
  text-align: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  position: relative;
  overflow: hidden;
}

.dark-theme .feature-card {
  background: rgba(26, 26, 46, 0.95);
  border-color: rgba(255, 255, 255, 0.1);
  color: #ecf0f1;
}

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
  transition: left 0.5s;
}

.feature-card:hover::before {
  left: 100%;
}

.feature-card:hover {
  transform: translateY(-10px) scale(1.02);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
}

.feature-icon {
  font-size: 3rem;
  margin-bottom: 20px;
  background: linear-gradient(135deg, #667eea, #764ba2);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.feature-title {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 15px;
  color: #2D2D2D;
}

.dark-theme .feature-title {
  color: #ecf0f1;
}

.feature-description {
  color: #666;
  line-height: 1.6;
  font-size: 1rem;
}

.dark-theme .feature-description {
  color: #bdc3c7;
}

/* What Do You Think Section */
.think-section {
  padding: 80px 20px;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
}

.dark-theme .think-section {
  background: rgba(0, 0, 0, 0.1);
}

.think-container {
  max-width: 1200px;
  margin: 0 auto;
  text-align: center;
}

.think-content {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.think-text {
  max-width: 800px;
  margin-bottom: 40px;
}

.think-text h3 {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 20px;
  color: white;
}

.think-text p {
  font-size: 1.2rem;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.6;
}

.think-actions {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}

.think-btn {
  padding: 15px 30px;
  border: none;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.think-btn-primary {
  background: linear-gradient(135deg, #f39c12, #e67e22);
  color: white;
  box-shadow: 0 8px 25px rgba(243, 156, 18, 0.3);
}

.think-btn-secondary {
  background: rgba(255, 255, 255, 0.2);
  color: white;
  border: 2px solid rgba(255, 255, 255, 0.3);
  backdrop-filter: blur(10px);
}

.think-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
}

.think-btn-primary:hover {
  background: linear-gradient(135deg, #e67e22, #d35400);
}

.think-btn-secondary:hover {
  background: rgba(255, 255, 255, 0.3);
  border-color: rgba(255, 255, 255, 0.5);
}

/* Call to Action Section */
.cta-section {
  padding: 80px 20px;
  text-align: center;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
}

.dark-theme .cta-section {
  background: rgba(0, 0, 0, 0.1);
}

.cta-container {
  max-width: 600px;
  margin: 0 auto;
}

.cta-title {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 20px;
  color: white;
}

.cta-description {
  font-size: 1.2rem;
  margin-bottom: 40px;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.6;
}

.cta-button {
  padding: 18px 40px;
  background: linear-gradient(135deg, #27ae60, #2ecc71);
  color: white;
  border: none;
  border-radius: 50px;
  font-size: 1.2rem;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: inline-flex;
  align-items: center;
  gap: 10px;
  box-shadow: 0 10px 30px rgba(39, 174, 96, 0.3);
}

.cta-button:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 40px rgba(39, 174, 96, 0.4);
  background: linear-gradient(135deg, #2ecc71, #27ae60);
}

/* Responsive Design */
@media (max-width: 768px) {
  .hero-title {
    font-size: 2.5rem;
  }
  
  .hero-subtitle {
    font-size: 1.1rem;
  }
  
  .hero-buttons {
    flex-direction: column;
    align-items: center;
  }
  
  .hero-btn {
    width: 100%;
    max-width: 300px;
    justify-content: center;
  }
  
  .section-title {
    font-size: 2rem;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
  }
  
  .think-text h3 {
    font-size: 2rem;
  }
  
  .think-actions {
    flex-direction: column;
    align-items: center;
  }
  
  .think-btn {
    width: 100%;
    max-width: 300px;
    justify-content: center;
  }
  
  .cta-title {
    font-size: 2rem;
  }
}

/* Animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.feature-card {
  animation: fadeInUp 0.6s ease-out;
}

.feature-card:nth-child(1) { animation-delay: 0.1s; }
.feature-card:nth-child(2) { animation-delay: 0.2s; }
.feature-card:nth-child(3) { animation-delay: 0.3s; }
.feature-card:nth-child(4) { animation-delay: 0.4s; }
.feature-card:nth-child(5) { animation-delay: 0.5s; }
.feature-card:nth-child(6) { animation-delay: 0.6s; }
</style>

<main class="home-container">
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-content">
      <h1 class="hero-title">Welcome to Book Notes 📖</h1>
      <p class="hero-subtitle">
        Your personal reading companion. Discover, track, and cherish your literary journey with our comprehensive book management platform.
      </p>
      <div class="hero-buttons">
        <% if (isAuthenticated) { %>
          <a href="/book" class="hero-btn hero-btn-primary">
            <i class="fa fa-search"></i>
            Start Reading
          </a>
          <a href="/myBooks" class="hero-btn hero-btn-secondary">
            <i class="fa fa-book"></i>
            My Library
          </a>
        <% } else { %>
          <a href="/account" class="hero-btn hero-btn-primary">
            <i class="fa fa-user-plus"></i>
            Get Started
          </a>
          <a href="/login" class="hero-btn hero-btn-secondary">
            <i class="fa fa-sign-in"></i>
            Sign In
          </a>
        <% } %>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features-section">
    <div class="features-container">
      <h2 class="section-title">✨ Why Choose Book Notes?</h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">🔍</div>
          <h3 class="feature-title">Smart Search</h3>
          <p class="feature-description">
            Find your next favorite book with our powerful search engine. Search by title, author, or explore trending books.
          </p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">📚</div>
          <h3 class="feature-title">Personal Library</h3>
          <p class="feature-description">
            Build and organize your personal book collection. Keep track of what you've read and what you want to read next.
          </p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">⭐</div>
          <h3 class="feature-title">Rate & Review</h3>
          <p class="feature-description">
            Share your thoughts with detailed ratings and reviews. Help others discover great books and track your reading progress.
          </p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">❤️</div>
          <h3 class="feature-title">Wishlist</h3>
          <p class="feature-description">
            Save books for later with your personal wishlist. Never lose track of books you want to read.
          </p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">🔔</div>
          <h3 class="feature-title">Smart Notifications</h3>
          <p class="feature-description">
            Stay updated with personalized notifications about your reading activity and new features.
          </p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">📱</div>
          <h3 class="feature-title">Modern Interface</h3>
          <p class="feature-description">
            Enjoy a beautiful, responsive design that works perfectly on all devices. Dark mode support included.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- What Do You Think Section -->
  <section class="think-section">
    <div class="think-container">
      <h2 class="section-title">🤔 What Do You Think?</h2>
      <div class="think-content">
        <div class="think-text">
          <% if (isAuthenticated) { %>
            <h3>Share Your Reading Journey</h3>
            <p>Every reader has a unique story. What books have changed your perspective? Which authors inspire you the most? Share your thoughts and discover new perspectives from fellow book lovers.</p>
          <% } else { %>
            <h3>Join Our Reading Community</h3>
            <p>Discover what makes Book Notes special. Join thousands of readers who are already tracking their reading journey, sharing reviews, and discovering new books. Start your own story today!</p>
          <% } %>
        </div>
        <div class="think-actions">
          <% if (isAuthenticated) { %>
            <a href="/book" class="think-btn think-btn-primary">
              <i class="fa fa-plus"></i>
              Add Your First Book
            </a>
            <a href="/myBooks" class="think-btn think-btn-secondary">
              <i class="fa fa-eye"></i>
              See Your Collection
            </a>
          <% } else { %>
            <a href="/account" class="think-btn think-btn-primary">
              <i class="fa fa-user-plus"></i>
              Create Account
            </a>
            <a href="/login" class="think-btn think-btn-secondary">
              <i class="fa fa-sign-in"></i>
              Sign In
            </a>
          <% } %>
        </div>
      </div>
    </div>
  </section>

  <!-- Call to Action Section -->
  <section class="cta-section">
    <div class="cta-container">
      <% if (isAuthenticated) { %>
        <h2 class="cta-title">Ready to Continue Your Reading Journey?</h2>
        <p class="cta-description">
          Keep exploring, reviewing, and discovering new books. Your reading adventure never ends!
        </p>
        <a href="/book" class="cta-button">
          <i class="fa fa-search"></i>
          Find New Books
        </a>
      <% } else { %>
        <h2 class="cta-title">Ready to Start Your Reading Journey?</h2>
        <p class="cta-description">
          Join thousands of readers who are already using Book Notes to discover, track, and enjoy their favorite books.
        </p>
        <a href="/account" class="cta-button">
          <i class="fa fa-rocket"></i>
          Get Started Now
        </a>
      <% } %>
    </div>
  </section>
</main>

<%- include('partials/footer.ejs') %>
